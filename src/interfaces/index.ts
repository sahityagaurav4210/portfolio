export interface IHeadingProps {
  headingName: string;
  className?: string;
}
export interface IHeroProps {
  url?: string;
}
export enum HiringType {
  PART_TIME = 'part time',
  FULL_TIME = 'full time',
}
